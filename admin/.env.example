# Port the dev server runs on
PORT=3011

# Base URL of the Bookd backend API
VITE_API_URL=

# Secret key used to encrypt session data (admin info/token) stored in localStorage — generate a strong random value, do not reuse across environments
VITE_ENCRYPT_DATA_KEY=
